Optional host-side install scripts. These are **not** run by `bootstrap/setup.sh`.

- `nvm.sh` — installs nvm and Node.js 24.18.0 for the invoking user (run manually when you need Node on the Pi host).
